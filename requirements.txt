# Application dependencies
flask==2.3.2
requests==2.28.0

# Intentionally outdated/vulnerable for demo scanning
# requests 2.28.0 has CVE-2023-32681 (Unintended leak of Proxy-Authorization header)
# This is deliberate — it gives the dependency scanner something to find.

# Jinja2 pinned low for additional scan findings
jinja2==3.1.6
